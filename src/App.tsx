/**
 * App.tsx
 *
 * Root application component for Startup Toolkit.
 * Renders AppRoutes client-side router (Landing Page, Workspace, Navbar, Sidebar, Privacy Policy).
 */

import { AppRoutes } from '@/routes/AppRoutes'

export default function App() {
  return <AppRoutes />
}
